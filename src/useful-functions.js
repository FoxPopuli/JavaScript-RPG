export function roll(n) {
  return Math.floor(Math.random() * n);
}

// export function toIndex(x, y, map) {
//     // returns index of corresponding map matrix position

//     return ((y * map.width) + x)
// }
